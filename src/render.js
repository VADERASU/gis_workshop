// This file contains the drawing code for the map. You can use any library you wish to render the map.
// Ask your coding agent to fill these functions in.
//   draw   ({ el, features, scale, label, format })
//   legend ({ el, scale, label, format })

export function draw({ el, features, scale, label, format }) {
    // TODO: have your agent fill this in
}

export function legend({ el, scale, format }) {
    // TODO: have your agent fill this in
}


